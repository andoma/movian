widget(container_x, {

  widget(label, {
    .aspect = 20;
    .align = right;
    .caption = $self.title + ":";
  });

  widget(text, {
    .focusCursor = true;
    .focusable = true;

    .aspect = 20;
    bind($self.value);
  });
});
