widget(container_x, {
  widget(label, {
    .aspect = 20;
    .align = right;
    .caption = self.title + ":";
  });

  widget(label, {
    .aspect = 20;
    .caption = "The value is " + int2str(self.value);
  });
});
