widget(container_x, {

  .focusCursor = true;
  .focus = target;

  onEvent(enter, genericEvent(navigator, open, $self.uri));

  widget(bitmap, {
    .keepAspect = true;
    .source =  translate($self.subtype,
			 [
			  "audio", "theme://icons/speaker.png",
			  "display", "theme://icons/screen.png"

			  ],
			 "theme://icons/config.png");
  });

  widget(label, {
    .aspect = 40;
    .weight = 10;
    .caption = $self.title;
  });
});
