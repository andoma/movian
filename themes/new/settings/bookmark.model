#include "theme://include/plates.model"

widget(container_x, {

  widget(backdrop, {
    HIGHLITE_PROPS();
      
    widget(text, {
      .align = left;
      .focusable = true;
      bind($self.title);
    });
  });

  widget(backdrop, {
    HIGHLITE_PROPS();
    
    widget(text, {
      .align = left;
      .focusable = true;
      bind($self.url);
    });
  });

  ACTION_BUTTON("Delete", "theme://icons/delete.png", {
    delete($self);
  });

});
