#include "theme://browser/views/default/plate.model"

BROWSER_PLATE_EXPAND("theme://images/movie.png",
	     
	      widget(container_x, {
		  widget(label, {.caption = media.title;});
		  widget(label, {.caption = media.time.total; .weight = 0.1;});
		});
	      ,

	      widget(container_y, {
		  widget(label, {.caption = media.format;});
		  widget(label, {.caption = media.videoinfo;});
		  widget(label, {.caption = media.audioinfo;});
		})
    );
