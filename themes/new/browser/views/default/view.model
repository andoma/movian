widget(container_y, {
    .displacement = [0, 0, 0.5];
    widget(form, {
	widget(list, {
	    .id = "node_container";
	    .orientation = vertical;
	    .slices = 30 / global.uiscale;
	    .expand = 3.0;
	  });
      });
  });
