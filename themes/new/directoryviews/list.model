#include "theme://include/plates.model"

widget(list_y, {
  .slices = 20;
  cloner($page.nodes, image, {
    BUTTON_PROPS(1.0);
    onEvent(enter, genericEvent(navigator, open, $self.url));
    
    widget(animator, {
      .time = 0.3;
      .source = "theme://directoryviews/list/" + 
	 $self.metadata.type + ".model";
    });
  });
});
