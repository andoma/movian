widget(container_x, {
  .aspect = 30;

  widget(image, {
    .source = "theme://icons/cd.png";
    .keepAspect = true; 
  });

  widget(label, {
    .weight = 14;
    .caption = $self.metadata.title;
  });
});
