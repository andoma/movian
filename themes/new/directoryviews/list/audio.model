
widget(container_x, {

  widget(image, {
    .source = "theme://icons/audio.png";
  });

  widget(container_y, {

    widget(label, {.caption = $self.metadata.title;});
    widget(label, {.size = 0.75; .caption = $self.metadata.author; });
    widget(label, {.size = 0.75; .caption = $self.metadata.album; });
  });

  widget(container_y, {
    .aspect = 2;
    widget(label, {
      .align = right;
      .caption = value2duration($self.metadata.duration);
    });
  });
});
