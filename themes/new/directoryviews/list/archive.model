widget(container_x, {
  .focusable = true;
  onEvent(enter, navOpen($self.url, $self.type));

  widget(image, {
    .source = "theme://icons/archive.png";
  });

  widget(label, {
    .caption = $self.metadata.title;
  });
});
