widget(container_x, {

  .debug = true;

  widget(image, {
    .source = "theme://icons/movie.png";
  });

  widget(container_y, {
    widget(label, {.caption = $self.metadata.title;});
  });

  widget(container_y, {
    .aspect = 2;
    widget(label, {
      .align = right;
      .caption = value2duration($self.metadata.duration);
    });
  });
});
