#include "theme://include/plates.model"


widget(container_y, {

  widget(list, {
    .weight = 10;
    .orientation = vertical;
    .slices = 10;
    cloner($page.nodes, container_z, {
      PLATE_WIDE(
        widget(animator, {
	  .source = "theme://settings/" + $self.type + ".model";
	});
      );
    });
  });

  widget(container_x, {
    BUTTON($page.mayadd, 2.0, "Add", "theme://images/new.png", {
      createChild($page.nodes);
    });
    space(6.0);
  });
});


