#include "theme://include/plates.model"

widget(container_y, {

  space(8);


  widget(container_x, {
    BUTTON(1, 1, "Settings", "theme://icons/config.png",
	   genericEvent(navigator, open, "settings:"));
    space(8);
  });
});
