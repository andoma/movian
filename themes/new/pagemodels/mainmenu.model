#include "theme://include/plates.model"

widget(container_y, {

  space(8);


  widget(stack_x, {
    BUTTON(1, 2, "Settings", "theme://icons/config.png",
	   genericEvent(navigator, open, "settings:"));

    BUTTON(1, 2, "Playqueue", "theme://icons/config.png",
	   genericEvent(navigator, open, "playqueue:"));
  });
});
