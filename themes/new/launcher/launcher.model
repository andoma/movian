#include "theme://include/hlist.model"

widget(form, {
    widget(container_y, {
	
	.displacement = [0 0 0.6];

	space(3.0);

	HLIST("Application Launcher", "application_container",
	      [0.5, 0.5, 0.5], 3);
      });
  });
