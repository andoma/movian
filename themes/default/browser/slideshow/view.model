#include "theme://include/playstatus.model"

widget(container_z, {
    .displacement = [0, 0, 1];
    widget(slideshow, {
	.id = "slideshow";
      });

    widget(container_y, {
	widget(container_x, {
	    PLAYSTATUS_WIDGET(slideshow.playstatus, 0.5);
	    space(15.0);
	  });
	space(15.0);
      });

  });
