widget(form, {
    widget(container_y, {
	.displacement = [0, 0, 0.9];
	widget(animator, {
	    .weight = 5.0;
	    .id = "preview_container";
	  });

	space(0.05);
	widget(list, {
	    .id = "node_container";
	    .orientation = horizontal;
	    .slices = 6;
	  });
	space(0.05);
      });
  });
