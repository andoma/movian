widget(container_y, {
    widget(label, { .caption = "Supported output formats:"; .weight = 0.33; });
    widget(label, { .caption = audiodevice.outputformats;   .weight = 0.66; });

    space(1);

    widget(list, {
	.orientation = vertical;
	.weight = 9;
	.id = "controllers";
	.slices = 9;
	.expand = 2;
      });
  });

