widget(container_x, {
  widget(label, {
    .width = 200;
    .autohide = true;
    .caption = $self.metadata.title + ":";
    .align = topRight;
    .color = 0.7;
  });

  widget(label, {
    .caption = $self.data;
    .align = left;
    .maxlines = 5;
  });
});
