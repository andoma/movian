#include "theme://include/separators.model"

widget(container_x, {

  widget(image, {
    .source = "theme://icons/32x32/page.png";
  });

  VERTICAL_SEPARATOR();

  widget(label, {
    .caption = $self.metadata.title;
  });
});
