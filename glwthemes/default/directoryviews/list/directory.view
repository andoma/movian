#include "theme://include/plates.view"
#include "theme://include/separators.view"

widget(backdrop, {
  HIGHLITE_PROPS();

  widget(container_x, {
    .focusable = true;
    onEvent(enter, navOpen($self.url));
    
    widget(image, {
      .source = "theme://icons/32x32/folder.png";
    });
    
    VERTICAL_SEPARATOR();

    widget(label, {
      .align = left; 
      .caption = $self.metadata.title;
      .ellipsize = true;
    });
  });
});