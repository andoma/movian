#include "theme://include/plates.view"

widget(container_x, {
    
  widget(label, {
    .align = right;
    .caption = $self.title;
  });
  
  widget(slider_x, {
    .focusable = true;
    .min = $self.min;
    .max = $self.max;
    .step = $self.step;
    bind($self.value);

    widget(backdrop, {
      HIGHLITE_PROPS();

      widget(label, {
	.caption = int2str($self.value) + $self.unit;
      });
    });
  });
});
