#include "theme://include/separators.model"
#include "theme://include/plates.model"

widget(container_y, {

  widget(container_x, {
    widget(list_y, {
      .id = "list";
      cloner($page.nodes, animator, {
	.time = 0.3;
	.source = "theme://directoryviews/list/track.model";
      });
    });
  
    widget(slider_y, {
      .width = SLIDER_WIDTH();
      .focusable = 0.2;
      bind("list");

      widget(backdrop, {
	KNOB_PROPS();
      });
    });
  });
});
