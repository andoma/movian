$view.font = loadFont($args.url);
filterConstraintY: true;

widget(container_y, {
  padding: 20;
  widget(label, {
    font: $view.font;
    caption: "The quick brown fox jumped over the lazy dog";
    maxlines: 3;
    sizeScale: 2;
  });
  space(1);
});
  