#include "theme://include/separators.view"
#include "theme://include/plates.view"

widget(container_y, {

  widget(container_x, {
    widget(list_y, {
      .id = "list";
      cloner($page.nodes, loader, {
	.time = 0.3;
	.source = "theme://directoryviews/list/track.view";
      });
    });
  
    widget(slider_y, {
      .width = SLIDER_WIDTH();
      .focusable = 0.2;
      bind("list");

      widget(backdrop, {
	KNOB_PROPS();
      });
    });
  });
});
