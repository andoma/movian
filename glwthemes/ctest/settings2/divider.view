
#import "theme://include/separators.view"

widget(container_y, {
  widget(dummy, { .height = 10;   });
  widget(label, {
    .caption = $self.model.metadata.title;
    .sizeScale = 1.5;
    .color = 0.5;
    .shadow = true;
  });

  HORIZONTAL_SEPARATOR();
});